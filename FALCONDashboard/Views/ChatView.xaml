<UserControl x:Class="FALCONDashboard.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="12" Background="#0a0c10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" x:Name="ChatScroll">
            <ItemsControl ItemsSource="{Binding Messages}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,4">
                            <TextBlock Text="{Binding Sender}" FontWeight="Bold" Foreground="#ff8a3d" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding Text}" Foreground="#eef1f6" TextWrapping="Wrap"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBox Grid.Row="1" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,8,0,0" Padding="6" FontSize="14"/>

        <Button Grid.Row="2" Content="送信" Command="{Binding SendCommand}"
                Margin="0,8,0,0" Padding="8,4" HorizontalAlignment="Right"/>
    </Grid>
</UserControl>